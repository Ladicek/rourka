The files here need to be `.jpi`, not `.hpi` -- at least if they are supposed
to override the plugins installed before.

- `github.jpi`: https://github.com/Ladicek/github-plugin/commits/JENKINS-31470
